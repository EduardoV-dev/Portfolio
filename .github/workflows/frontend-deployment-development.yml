name: Deploy Astro to AWS (Development Environment)

on:
    push:
        branches:
            - development
    workflow_dispatch:

permissions:
    id-token: write
    contents: read

concurrency:
    group: deploy-astro-dev
    cancel-in-progress: true

jobs:
    deploy:
        name: Deploy to development environment
        runs-on: ubuntu-latest
        environment: development
        defaults:
            run:
                working-directory: apps/web

        env:
            AWS_REGION: ${{ vars.AWS_REGION }}
            AWS_ROLE_ARN: ${{ vars.AWS_ROLE_ARN }}
            S3_BUCKET: ${{ vars.S3_BUCKET }}
            CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }}
            SITE_URL: ${{ vars.SITE_URL }}
            STRAPI_API_URL: ${{ vars.STRAPI_API_URL }}
            STRAPI_API_TOKEN: ${{ vars.STRAPI_API_TOKEN }}
            PUBLIC_WEB3FORMS_ACCESS_KEY: ${{ vars.PUBLIC_WEB3FORMS_ACCESS_KEY }}

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup pnpm
              uses: pnpm/action-setup@v4
              with:
                  version: 10

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: pnpm

            - name: Log env vars
              run: |
                  echo "STRAPI_API_URL=$STRAPI_API_URL"
                  echo "STRAPI_API_TOKEN=$([ -n "$STRAPI_API_TOKEN" ] && echo "set" || echo "NOT SET")"
                  echo "SITE_URL=$SITE_URL"
                  echo "PUBLIC_WEB3FORMS_ACCESS_KEY=$([ -n "$PUBLIC_WEB3FORMS_ACCESS_KEY" ] && echo "set" || echo "NOT SET")"

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Build Astro site
              run: pnpm build

            - name: Configure AWS credentials with OIDC
              uses: aws-actions/configure-aws-credentials@v4
              with:
                  aws-region: ${{ env.AWS_REGION }}
                  role-to-assume: ${{ env.AWS_ROLE_ARN }}
                  role-session-name: github-actions-astro-development

            - name: Upload non-HTML assets to S3
              run: |
                  aws s3 sync ./dist s3://${S3_BUCKET} \
                      --delete \
                      --exclude "*.html" \
                      --cache-control "public, max-age=31536000, immutable"

            - name: Upload HTML files to S3
              run: |
                  aws s3 sync ./dist s3://${S3_BUCKET} \
                      --exclude "*" \
                      --include "*.html" \
                      --cache-control "no-cache, no-store, must-revalidate"

            - name: Invalidate CloudFront cache
              run: |
                  aws cloudfront create-invalidation \
                      --distribution-id ${CLOUDFRONT_DISTRIBUTION_ID} \
                      --paths "/*"
