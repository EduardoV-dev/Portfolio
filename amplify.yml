version: 1
applications:
    - appRoot: apps/web
      frontend:
          buildPath: /
          phases:
              preBuild:
                  commands:
                      - npm i -g pnpm
                      - pnpm config set store-dir .pnpm-store
                      - pnpm install --frozen-lockfile
              build:
                  commands:
                      - env >> apps/web/.env
                      - pnpm --filter web build
                      - mv apps/web/.env apps/web/.amplify-hosting/compute/default/.env
          artifacts:
              baseDirectory: apps/web/.amplify-hosting
              files:
                  - "**/*"
          cache:
              paths:
                  - .pnpm-store/**/*
