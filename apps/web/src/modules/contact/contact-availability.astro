<section class="ca" aria-labelledby="ca-heading">
    <div class="ca__inner">
        <div class="ca__availability" data-animate>
            <div class="ca__status">
                <span class="ca__dot" aria-hidden="true"></span>
                <span class="ca__status-text">Available</span>
            </div>
            <p class="ca__label">AVAILABILITY</p>
            <h2 class="ca__heading" id="ca-heading">Currently taking on new work</h2>
            <p class="ca__desc">
                Open to select freelance engagements, full-time remote roles, and high-impact
                collaborations. Experienced working with U.S.&#8209;based teams and distributed
                environments.
            </p>
            <ul class="ca__details" aria-label="Availability details">
                <li class="ca__detail">
                    <span class="ca__detail-icon" aria-hidden="true">
                        <svg
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                        >
                            <circle cx="12" cy="12" r="10"></circle>
                            <polyline points="12 6 12 12 16 14"></polyline>
                        </svg>
                    </span>
                    <span>Timezone: CST (UTC&minus;6)</span>
                </li>
                <li class="ca__detail">
                    <span class="ca__detail-icon" aria-hidden="true">
                        <svg
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                        >
                            <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline>
                        </svg>
                    </span>
                    <span>Remote-first. Open to hybrid in select locations.</span>
                </li>
                <li class="ca__detail">
                    <span class="ca__detail-icon" aria-hidden="true">
                        <svg
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                        >
                            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                            <circle cx="9" cy="7" r="4"></circle>
                            <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                            <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
                        </svg>
                    </span>
                    <span>Works well with startups, scale-ups, and small engineering teams.</span>
                </li>
            </ul>
        </div>

        <div class="ca__closing" data-animate data-animate-delay="2">
            <p class="ca__closing-text">Have something in mind?</p>
            <p class="ca__closing-cta">
                <a href="#contact-options" class="ca__closing-link">Let's talk.</a>
            </p>
        </div>
    </div>
</section>

<style>
    @import "../../styles/mixins.css";

    .ca {
        position: relative;
        z-index: 1;
        padding-block: 2rem 6rem;
    }

    .ca__inner {
        max-width: 1280px;
        margin: 0 auto;
        padding-inline: 1rem;
        display: flex;
        flex-direction: column;
        gap: 3rem;
    }

    @mixin tablet-up {
        .ca__inner {
            padding-inline: 1.5rem;
        }
    }

    @mixin desktop-up {
        .ca__inner {
            flex-direction: row;
            align-items: flex-start;
            gap: 4rem;
        }

        .ca__availability {
            flex: 1;
        }

        .ca__closing {
            flex-shrink: 0;
            width: 20rem;
            padding-top: 0.5rem;
        }
    }

    /* ── Status badge ── */
    .ca__status {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.35rem 0.85rem;
        border-radius: 999px;
        background: rgba(34, 197, 94, 0.1);
        border: 1px solid rgba(34, 197, 94, 0.2);
        margin-bottom: 1.5rem;
    }

    .ca__dot {
        width: 0.5rem;
        height: 0.5rem;
        border-radius: 50%;
        background: #22c55e;
        animation: ca-pulse 2s cubic-bezier(0.37, 0, 0.63, 1) infinite;
    }

    .ca__status-text {
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        color: #22c55e;
    }

    /* ── Content ── */
    .ca__label {
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--color-accent);
        margin-bottom: 0.75rem;
    }

    .ca__heading {
        font-size: clamp(1.5rem, 4vw, 2rem);
        font-weight: 760;
        letter-spacing: -0.04em;
        color: var(--color-text-primary);
        line-height: 1.15;
        margin-bottom: 1rem;
        text-wrap: balance;
    }

    .ca__desc {
        font-size: clamp(0.9rem, 2vw, 1rem);
        color: var(--color-text-secondary);
        line-height: 1.7;
        max-width: 52ch;
        margin-bottom: 1.75rem;
    }

    /* ── Detail list ── */
    .ca__details {
        list-style: none;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .ca__detail {
        display: flex;
        align-items: flex-start;
        gap: 0.65rem;
        font-size: 0.875rem;
        color: var(--color-text-secondary);
        line-height: 1.5;
    }

    .ca__detail-icon {
        display: flex;
        align-items: center;
        color: var(--color-accent);
        opacity: 0.7;
        flex-shrink: 0;
        margin-top: 0.1rem;
    }

    /* ── Closing ── */
    .ca__closing {
        text-align: left;
    }

    @mixin desktop-up {
        .ca__closing {
            text-align: left;
        }
    }

    .ca__closing-text {
        font-size: clamp(1.1rem, 3vw, 1.35rem);
        font-weight: 700;
        color: var(--color-text-secondary);
        letter-spacing: -0.02em;
        line-height: 1.3;
    }

    .ca__closing-cta {
        margin-top: 0.25rem;
    }

    .ca__closing-link {
        font-size: clamp(1.4rem, 4vw, 2rem);
        font-weight: 800;
        color: var(--color-accent);
        letter-spacing: -0.04em;
        line-height: 1.2;
        text-decoration: underline;
        text-decoration-color: transparent;
        transition: text-decoration-color var(--transition-fast);
    }

    .ca__closing-link:hover {
        text-decoration-color: var(--color-accent);
    }

    .ca__closing-link:focus-visible {
        outline: 2px solid var(--color-accent);
        outline-offset: 3px;
        border-radius: 2px;
    }

    @keyframes ca-pulse {
        0%,
        100% {
            opacity: 1;
        }

        50% {
            opacity: 0.4;
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .ca__dot {
            animation: none;
        }

        .ca__closing-link {
            transition: none;
        }
    }
</style>

<script>
    import { initScrollReveal } from "@/utils/animations";
    initScrollReveal();
</script>
