<section class="ch" aria-labelledby="contact-hero-heading">
    <div class="ch__inner">
        <p class="ch__label">CONTACT</p>
        <h1 class="ch__title" id="contact-hero-heading">Let's build something great.</h1>
        <p class="ch__subtext">
            Open to select opportunities, collaborations, and high-impact projects.
        </p>
        <p class="ch__tagline">
            I help teams design and build scalable systems from the ground up.
        </p>
    </div>
</section>

<style>
    @import "../../styles/mixins.css";

    .ch {
        position: relative;
        z-index: 1;
        padding-block: 5rem 4rem;
        text-align: center;
    }

    .ch__inner {
        max-width: 1280px;
        margin: 0 auto;
        padding-inline: 1rem;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .ch__label {
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--color-accent);
        margin-bottom: 1rem;
        animation: ch-fade-up 500ms var(--ease-out-quint) both;
        animation-delay: 40ms;
    }

    .ch__title {
        font-size: clamp(2.5rem, 8vw, 4.5rem);
        font-weight: 800;
        letter-spacing: -0.05em;
        color: var(--color-text-primary);
        line-height: 1.05;
        margin-bottom: 1.5rem;
        max-width: 16ch;
        text-wrap: balance;
        animation: ch-fade-up 560ms var(--ease-out-quint) both;
        animation-delay: 120ms;
    }

    .ch__subtext {
        font-size: clamp(1rem, 2.5vw, 1.2rem);
        color: var(--color-text-secondary);
        line-height: 1.65;
        max-width: 44ch;
        text-wrap: pretty;
        animation: ch-fade-up 520ms var(--ease-out-quint) both;
        animation-delay: 200ms;
    }

    .ch__tagline {
        margin-top: 0.75rem;
        font-size: clamp(0.875rem, 2vw, 0.95rem);
        color: var(--color-text-secondary);
        opacity: 0.7;
        line-height: 1.6;
        max-width: 50ch;
        text-wrap: pretty;
        animation: ch-fade-up 520ms var(--ease-out-quint) both;
        animation-delay: 280ms;
    }

    @mixin tablet-up {
        .ch {
            padding-block: 6rem 4.5rem;
        }

        .ch__inner {
            padding-inline: 1.5rem;
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .ch__label,
        .ch__title,
        .ch__subtext,
        .ch__tagline {
            animation: none;
        }
    }

    @keyframes ch-fade-up {
        from {
            opacity: 0;
            transform: translateY(18px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>
