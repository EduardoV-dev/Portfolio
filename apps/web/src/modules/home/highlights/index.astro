---
import productionIcon from "../assets/production-engineering.svg";
import awsIcon from "../assets/aws-cloud.svg";
import architectureIcon from "../assets/system-architecture.svg";
import remoteIcon from "../assets/remote-first.svg";
import HighlightsCarousel from "./highlights-carousel";
import { YEARS_OF_PROFESSIONAL_EXPERIENCE } from "@/constants/common";

const items = [
    {
        icon: productionIcon,
        title: "Full Stack Ownership",
        description: `${YEARS_OF_PROFESSIONAL_EXPERIENCE}+ years shipping end-to-end digital products`,
    },
    {
        icon: awsIcon,
        title: "AWS Cloud & DevOps",
        description: "Reliable deployments, stable performance, CI/CD pipelines",
    },
    {
        icon: architectureIcon,
        title: "Product & Marketing Focus",
        description: "Bridging engineering, product, and marketing teams",
    },
    {
        icon: remoteIcon,
        title: "US Remote Ready",
        description: "Async-first, timezone-aligned with US teams",
    },
];

const carouselItems = items.map((item) => ({
    iconSrc: item.icon.src,
    iconWidth: item.icon.width,
    iconHeight: item.icon.height,
    title: item.title,
    description: item.description,
}));
---

<section class="highlights" aria-label="Key highlights">
    <div class="highlights__inner layout-container">
        {/* Mobile: Embla carousel */}
        <div class="highlights__carousel">
            <HighlightsCarousel client:load items={carouselItems} />
        </div>

        {/* Desktop: CSS grid */}
        <ul class="highlights__grid" role="list">
            {
                items.map((item) => (
                    <li class="highlights__item">
                        <div class="highlights__icon-box" aria-hidden="true">
                            <img
                                src={item.icon.src}
                                alt=""
                                width={item.icon.width}
                                height={item.icon.height}
                                loading="lazy"
                                decoding="async"
                            />
                        </div>
                        <div class="highlights__text">
                            <p class="highlights__title">{item.title}</p>
                            <p class="highlights__desc">{item.description}</p>
                        </div>
                    </li>
                ))
            }
        </ul>
    </div>
</section>

<style>
    @import "../../../styles/mixins.css";

    .highlights {
        position: relative;
        z-index: 1;
        margin-top: 1.25rem;
    }

    .highlights__inner {
        border: 1px solid var(--color-border);
        border-radius: 18px;
        background:
            linear-gradient(180deg, rgba(14, 23, 34, 0.82), rgba(10, 17, 25, 0.86)),
            radial-gradient(120% 80% at 50% 0%, rgba(109, 177, 232, 0.1), transparent 70%);
        overflow: clip;
    }

    /* ── Mobile: show carousel, hide grid ── */
    .highlights__carousel {
        display: block;
        padding: 1.2rem 0;
    }

    .highlights__grid {
        display: none;
    }

    /* ── Desktop: hide carousel, show grid ── */
    @mixin desktop-up {
        .highlights__carousel {
            display: none;
        }

        .highlights__grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            list-style: none;
            gap: 0;
        }

        .highlights__item {
            display: flex;
            flex-direction: row;
            align-items: center;
            gap: 1rem;
            padding: 2rem 1.5rem;
            border-right: 1px solid rgba(153, 182, 212, 0.14);
        }

        .highlights__item:last-child {
            border-right: none;
        }
    }

    /* Icon box — desktop grid */
    .highlights__icon-box {
        position: relative;
        flex-shrink: 0;
        width: 3.2rem;
        height: 3.2rem;
        border-radius: 0.85rem;
        border: 1px solid rgba(140, 185, 224, 0.38);
        background: linear-gradient(180deg, rgba(16, 27, 39, 0.95), rgba(12, 20, 30, 0.98));
        display: grid;
        place-items: center;
        box-shadow:
            0 0 0 1px rgba(255, 255, 255, 0.015) inset,
            0 8px 20px rgba(2, 7, 13, 0.45);
    }

    .highlights__icon-box::before {
        content: "";
        position: absolute;
        width: 60%;
        aspect-ratio: 1 / 1;
        border-radius: 50%;
        background: rgba(19, 65, 97, 0.55);
        pointer-events: none;
    }

    .highlights__icon-box img {
        position: relative;
        z-index: 1;
        width: 1.25rem;
        height: 1.25rem;
        object-fit: contain;
    }

    .highlights__text {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
    }

    .highlights__title {
        font-size: 0.88rem;
        font-weight: 700;
        color: var(--color-text-primary);
        letter-spacing: 0.03em;
        text-transform: uppercase;
        line-height: 1.2;
    }

    .highlights__desc {
        font-size: 0.8rem;
        color: var(--color-text-secondary);
        line-height: 1.55;
    }
</style>
