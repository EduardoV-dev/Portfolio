---
export interface Props {
    iconSrc: string;
    iconWidth: number;
    iconHeight: number;
    title: string;
    description: string;
}

const { iconSrc, iconWidth, iconHeight, title, description } = Astro.props;
---

<li class="phil-card">
    <div class="phil-card__icon-box" aria-hidden="true">
        <img
            src={iconSrc}
            alt=""
            width={iconWidth}
            height={iconHeight}
            loading="lazy"
            decoding="async"
        />
    </div>
    <h3 class="phil-card__title">{title}</h3>
    <p class="phil-card__desc">{description}</p>
</li>

<style>
    @import "../../../styles/mixins.css";

    .phil-card {
        display: flex;
        flex-direction: column;
        gap: 0;
        padding: 1.75rem;
        background: linear-gradient(180deg, rgba(17, 27, 40, 0.85), rgba(11, 19, 29, 0.88));
        border: 1px solid var(--color-border);
        border-radius: 1rem;
        transition:
            transform 220ms cubic-bezier(0.22, 1, 0.36, 1),
            border-color 220ms ease,
            box-shadow 220ms ease;
    }

    .phil-card:hover {
        transform: translateY(-3px);
        border-color: rgba(120, 196, 247, 0.44);
        box-shadow:
            0 12px 32px rgba(0, 0, 0, 0.3),
            0 0 0 1px rgba(120, 196, 247, 0.11) inset;
    }

    .phil-card:hover .phil-card__icon-box {
        border-color: rgba(0, 204, 255, 0.55);
        box-shadow:
            0 0 0 1px rgba(255, 255, 255, 0.02) inset,
            0 8px 20px rgba(2, 7, 13, 0.45),
            0 0 12px rgba(0, 204, 255, 0.18);
    }

    @media (prefers-reduced-motion: reduce) {
        .phil-card {
            transition: none;
        }
    }

    @mixin desktop-up {
        .phil-card {
            padding: 2rem;
        }
    }

    /* ── Icon box ── */
    .phil-card__icon-box {
        position: relative;
        flex-shrink: 0;
        width: 3.5rem;
        height: 3.5rem;
        border-radius: 0.9rem;
        border: 1px solid rgba(127, 185, 230, 0.4);
        background: linear-gradient(180deg, rgba(8, 14, 21, 0.97), rgba(6, 11, 18, 0.99));
        display: grid;
        place-items: center;
        margin-bottom: 1.5rem;
        box-shadow:
            0 0 0 1px rgba(255, 255, 255, 0.015) inset,
            0 8px 20px rgba(2, 7, 13, 0.45);
    }

    .phil-card__icon-box::before {
        content: "";
        position: absolute;
        width: 60%;
        aspect-ratio: 1 / 1;
        border-radius: 50%;
        background: rgba(0, 60, 80, 0.55);
        pointer-events: none;
    }

    .phil-card__icon-box img {
        position: relative;
        z-index: 1;
        width: 1.4rem;
        height: 1.4rem;
        object-fit: contain;
    }

    /* ── Title ── */
    .phil-card__title {
        font-size: 0.92rem;
        font-weight: 700;
        color: var(--color-text-primary);
        letter-spacing: 0.03em;
        text-transform: uppercase;
        line-height: 1.3;
        margin-bottom: 1rem;
    }

    @mixin desktop-up {
        .phil-card__title {
            font-size: 1.05rem;
        }
    }

    /* ── Description ── */
    .phil-card__desc {
        font-size: 0.9rem;
        color: var(--color-text-secondary);
        line-height: 1.65;
    }
</style>
